pip install openai

from openai import OpenAI
client = OpenAI(api_key="XXXXXXXXXXXXXXXXX")


def call_llm(messages, model="gpt-4o-mini", temperature = 0.7):
  response = client.chat.completions.create(
  model = model,
  messages = messages,
  temperature = temperature
  )
  return response.choices[0].message.content

messages = [
    {
        "role": "user" ,
        "content": "Explain GenAI"
    }
]
print(call_llm(messages))

messages = [
    {
        "role": "user" ,
        "content": (
            "Summarize following text in 5 Points.\n"
            "text : GenAI is Emerging Field"
        )
    }
]
print(call_llm(messages))
